#加载模块
import random
import time

#自定义函数
def 
#开始进入Python的世界
global passwordpadd, password, passwordadd, power_on_time, power_on
print(('启动时间：' + time.ctime()))
power_on = 'False'
if (power_on == 'True'):
    power_on = random.randint(5, 30)
    print((time.ctime() + ('本次启动时长' + power_on_time)))
else:
    print('第一次启动，设置新用户Administrator')
    passwordadd = input('请输入密码')
    passwordpadd = input('请输入密码')
    if (passwordadd == passwordpadd):
        password = input()
        print((time.ctime() + ('密码设置成功，请记好您的密码：' + passwordadd)))
    else:
        pass
